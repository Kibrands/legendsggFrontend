import { LevelBorderConverterPipe } from './level-border-converter.pipe';

describe('LevelBorderConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelBorderConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
